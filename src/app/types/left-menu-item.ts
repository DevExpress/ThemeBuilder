import { MetaItem } from './meta-item';

export class LeftMenuItem {
    order: number;
    unitedGroupKey: string;
    groupName: string;
    equivalents: string;
    items: Array<MetaItem>;
}
