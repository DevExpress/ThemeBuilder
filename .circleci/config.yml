version: 2
jobs:
  build:
    branches:
      ignore:
        - gh-pages
    docker:
      - image: circleci/node:8

    steps:
      - checkout

      - run:
          name: Install packages
          command: npm install
      - run:
          name: Run linter
          command: npm run lint
